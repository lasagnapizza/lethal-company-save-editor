<!DOCTYPE html>
<html>
  <head>
    <title>LCSaveEditor v1.0</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= preload_link_tag "/assets/PerfectDOSVGA437.woff"  %>
    <%= preload_link_tag "/assets/PerfectDOSVGA437.woff2"  %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="scanlines" data-controller="background">
    <div class="wrapper">
      <div data-simplebar>
        <div>
          <%= link_to "LCSaveEditor v1.0", root_path %> |
          <%= link_to "all saves", saves_path %> |
          <% if Current.user.blank? %>
            <%= link_to "sign in", new_session_path %> |
          <% end %>
          <%= link_to "help", help_path %>
        </div>

        <% if Current.user.present? %>
          <p>> You are logged in as <%= link_to Current.user.username, Current.user %></p>
        <% end %>


        <% flash.each do |type, message| %>
          <p class="flash <%= type %>">
            > <%= message %>
          </p>
        <% end %>

        <%= yield %>
      </div>
    </div>
  </body>
</html>
